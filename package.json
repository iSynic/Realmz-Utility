{
  "name": "realmz-scenario-utility",
  "productName": "Realmz Scenario Utility",
  "version": "0.1.3",
  "description": "Desktop utility for inspecting Realmz scenario data.",
  "author": "Realmz Scenario Utility contributors",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "tauri dev",
    "tauri": "tauri",
    "desktop": "tauri dev",
    "prepare:runtime": "node scripts/prepare-runtime-assets.mjs",
    "dist": "npm run prepare:runtime && tauri build",
    "dist:win": "npm run prepare:runtime && tauri build --bundles nsis",
    "dist:linux": "npm run prepare:runtime && tauri build --bundles appimage"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
